# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ amar5607-patch-1 ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8.
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: |
            mvn -B package --file pom.xml
    - name: Upload math result for job 2
      uses: actions/upload-artifact@master
      with:
          name: testing1
          path: .
    - name: Build with Maven
      uses: amar5607/maven_testing_1@master
        run:rsync -avz -e "ssh -i ${{ secrets.SSH_KEY }} -o StrictHostKeyChecking=no" ./testing1/target/ vagrant@172.42.42.101:/home/vagrant
      env:
        my_ssh_key: ${{ secrets.SSH_KEY }}
